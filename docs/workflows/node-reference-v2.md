# Workflow Node Reference

This compatibility path now points to the registry-generated [Workflow Node Reference](./node-reference.md).

The generated reference is the single source of truth for supported and unsupported node types, fields, ports, and runtime help. Run `wardian workflow gen-docs --check` to verify that registry changes are reflected there.
